package mx.com.javaudemy;

/*
 * Cuando trabajamos en proyectos del mundo real, lo común es utilizar el 
 * nombre de nuestro dominio. Sin embargo, lo común en JAVA cuando trabajamos
 * con paquetes, es estar escrito en minúsculas e invertir el nombre del dominio.
 * 
 * Si no poseemos ningún dominio, podemos asignar un nombre al proyecto y crear
 * imaginariamente nuestro nombre de internet. La idea es que sigamos este
 * estándar, aunque no tengamos un dominio en internet, ya que este dominio web
 * no es necesario que exista en internet, sólo es para evitar conflictos de nombre.
 */

public class Utileria {

    public static void imprimir(String cadena) {

        System.out.println(cadena);
    }
}
